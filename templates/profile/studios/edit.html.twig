{% extends 'profile/layout.html.twig' %}

{% block title %}{{studio.name ~ title_separator ~ site_name }}{% endblock %}

{% block content %}

    <h2>{{ 'Edit studios'|trans }}</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn-secondary btn">{{ 'Edit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}