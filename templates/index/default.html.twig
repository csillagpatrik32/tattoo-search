{% extends 'base-layout.html.twig' %}

{% block content %}
    {%  if message_count == 0 %}
        <h3>{{ 'Oops'|trans }}!</h3>
        <p>{{ 'Something went wrong.'|trans }} <a href="{{ path('index') }}">{{ 'Back to the main page.'|trans }}</a></p>
    {% endif %}
{% endblock %}