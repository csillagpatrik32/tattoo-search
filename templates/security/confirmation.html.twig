{% extends 'layout.html.twig' %}

{% block title %}{{site_name}}{% endblock %}

{% block body %}
    {% if user %}
        <h5>Congratulations!</h5>
        <p>Your account is confirmed! You can now sign in
            <a href="{{ path('security_login') }}">here</a></p>
    {% endif %}
{% endblock %}
