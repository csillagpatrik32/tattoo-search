{% extends 'profile/layout.html.twig' %}

{% block title %}{{'Add new employee'| trans ~ title_separator ~ site_name }}{% endblock %}

{% block content %}

    <h2>{{ 'Add new employee'|trans }}</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn-secondary btn">{{ 'Submit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}